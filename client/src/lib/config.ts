/** Adresse du site public. Le lien « Voir le site » et les retours du pied de page
 *  y renvoient ; à faire pointer sur la façade client une fois celle-ci en ligne. */
export const PUBLIC_SITE_URL = import.meta.env.VITE_PUBLIC_SITE_URL ?? '/';
